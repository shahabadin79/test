name: Manually Deploy to Heroku

on: workflow_dispatch


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "54bd11bf-194d-4860-a01c-8f717c6c13e6"
          heroku_email: "shahabadin01@gmail.com"
          heroku_app_name: "sorrzot" #Must be unique in Heroku
          region: "eu"
